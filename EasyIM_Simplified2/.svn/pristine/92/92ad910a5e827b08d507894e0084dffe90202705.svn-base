//
//  YKUserInfo.m
//  kin
//
//  Created by 源库 on 2017/3/4.
//  Copyright © 2017年 yuanku. All rights reserved.
//

#import "YKUserInfo.h"

//对字符串做特殊的宏，即保证返回的值不为空
#define ISNULL(x) ((x) == nil || [(x) isEqualToString:@"NULL"] || [(x) isEqualToString:@"null"] ? @"" : (x))

@implementation YKUserInfo

//返回一个 Dict，将 Model 属性名对映射到 JSON 的 Key。
+ (NSDictionary *)modelCustomPropertyMapper {
    return @{
             @"token" : @"data",
             };
}

/** 有昵称返回昵称，没有则返回用户名 */
-(NSString *)getUName{
    if (self.nickName.checkTextEmpty) {
        return self.nickName;
    }
    
    return self.userName;
}

@end
